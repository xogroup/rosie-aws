require 'spec_helper'

describe Rosie::Aws do
  it 'has a version number' do
    expect(Rosie::Aws::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
